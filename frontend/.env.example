# Backend API configuration
# This should be the URL of your backend service (e.g., Cloud Run URL)
BACKEND_API_URL=https://your-backend-service-xxxxx.a.run.app

# For local development, you can use:
# BACKEND_API_URL=http://localhost:8080

# Google Cloud Authentication
# When running locally, ensure you have Application Default Credentials set up:
# gcloud auth application-default login
# 
# When deployed, the service should use the service account's credentials automatically.

# Optional: Enable mock responses for testing
# USE_MOCK_RESPONSES=true